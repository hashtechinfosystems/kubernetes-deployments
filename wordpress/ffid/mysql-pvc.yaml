apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ffid-mysql-pvc
  namespace: ffid
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  volumeName: ffid-mysql-pv
  storageClassName: microk8s-hostpath