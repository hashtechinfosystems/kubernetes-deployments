apiVersion: apps/v1
kind: Deployment
metadata:
  name: ffid-wordpress
  namespace: ffid
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ffid-wordpress
  template:
    metadata:
      name: ffid-wordpress
      labels:
        app: ffid-wordpress
    spec:
      containers:
      - name: ffid-wordpress
        image: wordpress:6.5-php8.3-apache
        envFrom:
        - configMapRef:
            name: wp-config
        - secretRef:
            name: wp-secret
        ports:
        - containerPort: 80
        volumeMounts:
        - name: wp-persistent-storage
          mountPath: /var/www/html
      volumes:
      - name: wp-persistent-storage
        persistentVolumeClaim:
          claimName: ffid-wp-pvc
