apiVersion: apps/v1
kind: Deployment
metadata:
  name: pflg-mysql
  namespace: pflg
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pflg-mysql
  template:
    metadata:
      labels:
        app: pflg-mysql
    spec:
      containers:
      - name: pflg-mysql
        image: mysql:5.7
        envFrom:
        - configMapRef:
            name: mysql-config
        - secretRef:
            name: mysql-secret
        ports:
        - containerPort: 3306
        resources:
          requests:
            memory: 128Mi
            cpu: 250m
          limits:
            memory: 512Mi
            cpu: 500m
        volumeMounts:
        - name: pflg-mysql-volume
          mountPath: /var/lib/mysql
      volumes:
      - name: pflg-mysql-volume
        persistentVolumeClaim:
          claimName: pflg-mysql-pvc