apiVersion: v1
kind: Service
metadata:
  name: wp-svc
  namespace: calid
spec:
  selector:
    app: calid-wordpress
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 31001