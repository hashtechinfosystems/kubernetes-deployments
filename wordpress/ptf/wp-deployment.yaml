apiVersion: apps/v1
kind: Deployment
metadata:
  name: ptf-wordpress
  namespace: ptf
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ptf-wordpress
  template:
    metadata:
      name: ptf-wordpress
      labels:
        app: ptf-wordpress
    spec:
      containers:
      - name: ptf-wordpress
        image: wordpress:6.5-php8.3-apache
        envFrom:
        - configMapRef:
            name: wp-config
        - secretRef:
            name: wp-secret
        ports:
        - containerPort: 80
        volumeMounts:
        - name: wp-persistent-storage
          mountPath: /var/www/html
      volumes:
      - name: wp-persistent-storage
        persistentVolumeClaim:
          claimName: ptf-wp-pvc