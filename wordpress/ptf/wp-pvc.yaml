apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ptf-wp-pvc
  namespace: ptf
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  volumeName: ptf-wp-pv
  storageClassName: microk8s-hostpath