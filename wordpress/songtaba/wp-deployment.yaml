apiVersion: apps/v1
kind: Deployment
metadata:
  name: songtaba-wordpress
  namespace: songtaba
spec:
  replicas: 1
  selector:
    matchLabels:
      app: songtaba-wordpress
  template:
    metadata:
      name: songtaba-wordpress
      labels:
        app: songtaba-wordpress
    spec:
      containers:
      - name: songtaba-wordpress
        image: wordpress:6.5-php8.3-apache
        envFrom:
        - configMapRef:
            name: wp-config
        - secretRef:
            name: wp-secret
        ports:
        - containerPort: 80
        resources:
          requests:
            memory: 128Mi
            cpu: 250m
          limits:
            memory: 512Mi
            cpu: 500m
        volumeMounts:
        - name: wp-persistent-storage
          mountPath: /var/www/html
      volumes:
      - name: wp-persistent-storage
        persistentVolumeClaim:
          claimName: songtaba-wp-pvc