apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: calid-wp-pvc
  namespace: calid
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  volumeName: calid-wp-pv
  storageClassName: microk8s-hostpath