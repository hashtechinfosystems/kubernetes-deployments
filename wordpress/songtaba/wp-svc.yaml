apiVersion: v1
kind: Service
metadata:
  name: wp-svc
  namespace: songtaba
spec:
  selector:
    app: songtaba-wordpress
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 31006